- uses: lowlighter/metrics@latest
        with:
          user: Olikere
          token: ${{ secrets.GH_TOKEN }}

          # Configuração para focar apenas nas linguagens
          base: metadata 
          plugin_languages: yes
          plugin_languages_details: bytes-size, percentage
          plugin_languages_threshold: 0%
          plugin_isocalendar: no
          plugin_habits: no

          # 💡 NOVO: Defina o tema escuro para harmonizar as cores
          config_octocat: yes # Mantém o ícone do octocat no header (opcional)
          config_padding: 0 # Remove padding em volta (opcional)
          
          # Escolha um tema escuro. Tente 'onedark', 'dark' ou 'gruvbox'.
          # 👇👇👇
          config_base: onedark 
          # 👆👆👆

          config_timezone: America/Sao_Paulo
          output_dir: .
